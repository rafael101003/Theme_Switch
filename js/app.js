function btnClaro() {
    let claro = document.body.classList.add("temaClaro").value || [];

    console.log(claro);
}
function btnEscuro(){
    let escuro = document.body.classList.add("temaEscuro").value || [];
    
    console.log(escuro);
}